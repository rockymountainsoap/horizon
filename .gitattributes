# =============================================================================
# WS7 (2026-07): the merge=ours registry is RETIRED. This repo is Rocky-owned
# by default; upstream sync is limited to the 14-file engine manifest and
# happens via manifest-scoped cherry-picks, never `git merge upstream/main`.
#
#   Ownership model .... .cursor/plans/WS7-frontend-rework-adr.md
#   Tracked files ...... .cursor/references/engine-manifest.md
#   Sync workflow ...... .claude/skills/engine-sync/SKILL.md
#
# Do NOT re-add merge=ours entries. The old registry is in git history and the
# tombstoned .cursor/references/gitattributes-merge-strategy.md.
# The `git config merge.ours.driver true` setting is now inert; safe to remove
# per clone (git config --unset merge.ours.driver).
# =============================================================================
