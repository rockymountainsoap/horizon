# rocky-wishlist-app

WS0 **native wishlist** Shopify app (App Proxy, Customer Account UI, theme app block). See **`../README.md`** for layout and setup.

Quick start: edit **`shopify.app.toml`**, deploy **`../../workers/native_worker`**, then `npm install` and `shopify app dev` from this directory.
